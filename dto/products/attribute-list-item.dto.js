function AttributeListItemDto(id, size, color, manufacturer, season, rating) {
    return {
        id,
        size,
        color,
        manufacturer,
        season,
        rating
    }
}

module.exports = AttributeListItemDto
